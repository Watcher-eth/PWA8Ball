import { ChainType, Erc20Amount, FiatAmount } from '@lens-protocol/shared-kernel';
import { Amount as ApiAmount, FiatAmount as ApiFiatAmount } from "../graphql/generated.js";
export declare function chainType(chainId: number): ChainType;
/**
 * Convert an Amount returned from the Lens API to an Erc20Amount
 *
 * @param from - Amount returned from the Lens API
 * @returns Erc20Amount instance
 */
export declare function erc20Amount(from: ApiAmount): Erc20Amount;
/**
 * Convert a FiatAmount returned from the Lens API to a FiatAmount
 *
 * @param from - FiatAmount returned from the Lens API
 * @returns FiatAmount instance
 */
export declare function fiatAmount(from: ApiFiatAmount): FiatAmount;
