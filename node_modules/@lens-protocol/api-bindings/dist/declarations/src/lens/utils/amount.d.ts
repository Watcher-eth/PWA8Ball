import { ChainType, Erc20Amount } from '@lens-protocol/shared-kernel';
import { Amount as ApiAmount } from "../graphql/generated.js";
export declare function chainType(chainId: number): ChainType;
export declare function erc20Amount({ from }: {
    from: ApiAmount;
}): Erc20Amount;
