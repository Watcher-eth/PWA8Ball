import { MockedResponse } from '@apollo/client/testing';
import { EvmAddress } from '@lens-protocol/shared-kernel';
import { LensTransactionResult, LensTransactionStatusData, LensTransactionStatusRequest } from "../../graphql/generated.js";
export declare function mockLensTransactionStatusDataResponse({ request, result, }: {
    request: LensTransactionStatusRequest;
    result: LensTransactionResult | null;
}): MockedResponse<LensTransactionStatusData>;
export declare function mockLensTransactionResult(overrides?: Partial<LensTransactionResult>): LensTransactionResult;
export declare function mockGenerateLensAPIRelayAddressResponse({ address }: {
    address: EvmAddress;
}): {
    request: {
        query: import("graphql").DocumentNode;
    };
    result: {
        data: {
            result: string;
        };
    };
};
