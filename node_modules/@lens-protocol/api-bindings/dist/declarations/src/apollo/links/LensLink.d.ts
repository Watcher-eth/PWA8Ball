import { HttpLink } from '@apollo/client';
import { ILogger } from '@lens-protocol/shared-kernel';
import { SemVer } from "../../SemVer.js";
type Fetch = typeof fetch;
export type LensLinkArgs = {
    fetch?: Fetch;
    logger: ILogger;
    origin?: string;
    supportedVersion: SemVer;
    uri: string;
};
export declare function createLensLink({ fetch: preferredFetch, logger, origin, supportedVersion, uri, }: LensLinkArgs): HttpLink;
export {};
