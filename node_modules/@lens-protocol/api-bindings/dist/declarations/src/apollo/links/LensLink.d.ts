import { HttpLink } from '@apollo/client';
import { ILogger } from '@lens-protocol/shared-kernel';
import { SemVer } from "../../SemVer.js";
export type LensLinkArgs = {
    fetch?: WindowOrWorkerGlobalScope['fetch'];
    logger: ILogger;
    supportedVersion: SemVer;
    uri: string;
};
export declare function createLensLink({ fetch: preferredFetch, logger, supportedVersion, uri, }: LensLinkArgs): HttpLink;
export type SnapshotLinkArgs = {
    uri: string;
};
export declare function createSnapshotLink({ uri }: SnapshotLinkArgs): HttpLink;
