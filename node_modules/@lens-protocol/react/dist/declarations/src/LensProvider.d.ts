import { ReactNode } from 'react';
import { LensConfig } from "./config.js";
/**
 * <LensProvider> props
 */
export type LensProviderProps = {
    /**
     * The children to render
     */
    children: ReactNode;
    /**
     * The configuration for the Lens SDK
     */
    config: LensConfig;
};
/**
 * Manages the lifecycle and internal state of the Lens SDK
 *
 * @group Components
 * @param props - {@link LensProviderProps}
 */
export declare function LensProvider({ children, ...props }: LensProviderProps): import("react/jsx-runtime").JSX.Element;
