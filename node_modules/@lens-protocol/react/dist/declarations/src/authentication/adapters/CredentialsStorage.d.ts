import { IStorage, IStorageProvider, StorageSubscriber, StorageSubscription, PersistedCredentials } from '@lens-protocol/storage';
import { Credentials } from "./Credentials.js";
/**
 * Stores auth credentials.
 * Access token is kept in memory.
 * Refresh token is persisted permanently.
 */
export declare class CredentialsStorage implements IStorage<Credentials> {
    refreshTokenStorage: IStorage<PersistedCredentials>;
    accessToken: string | null;
    constructor(storageProvider: IStorageProvider, namespace: string);
    set({ accessToken, refreshToken }: Credentials): Promise<void>;
    get(): Promise<Credentials | null>;
    reset(): Promise<void>;
    subscribe(_: StorageSubscriber<Credentials>): StorageSubscription;
    getAccessToken(): string | null;
    private getRefreshToken;
}
