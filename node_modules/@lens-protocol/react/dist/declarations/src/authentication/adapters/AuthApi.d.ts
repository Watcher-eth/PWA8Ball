import { SafeApolloClient, ChallengeRequest, SignedAuthChallenge } from '@lens-protocol/api-bindings';
import { Credentials } from "./Credentials.js";
export type AuthChallenge = {
    id: string;
    text: string;
};
export declare class AuthApi {
    private apolloClient;
    constructor(apolloClient: SafeApolloClient);
    generateChallenge(request: ChallengeRequest): Promise<AuthChallenge>;
    generateCredentials(request: SignedAuthChallenge): Promise<Credentials>;
    refreshCredentials(refreshToken: string): Promise<Credentials>;
}
