import { SafeApolloClient } from '@lens-protocol/api-bindings';
import { ICredentialsWriter, ICredentialsReader, IResettableCredentialsGateway, LogoutReason } from '@lens-protocol/domain/use-cases/authentication';
import { IStorage } from '@lens-protocol/storage';
import { Credentials } from "./Credentials.js";
export declare class CredentialsGateway implements ICredentialsWriter, ICredentialsReader, IResettableCredentialsGateway {
    private readonly credentialsStorage;
    private apolloClient;
    constructor(credentialsStorage: IStorage<Credentials>, apolloClient: SafeApolloClient);
    getCredentials(): Promise<Credentials | null>;
    save(credentials: Credentials): Promise<void>;
    invalidate(reason: LogoutReason): Promise<void>;
    private revoke;
}
