import { ProfileId, Wallet } from '@lens-protocol/domain/entities';
import { ICredentialsIssuer, LoginError, CredentialsExpiredError, ICredentialsRenewer } from '@lens-protocol/domain/use-cases/authentication';
import { PromiseResult } from '@lens-protocol/shared-kernel';
import { AuthApi } from "./AuthApi.js";
import { Credentials } from "./Credentials.js";
export declare class CredentialsFactory implements ICredentialsIssuer, ICredentialsRenewer {
    private auth;
    constructor(auth: AuthApi);
    renewCredentials(credentials: Credentials): PromiseResult<Credentials, CredentialsExpiredError>;
    issueCredentials(signer: Wallet, using?: ProfileId): PromiseResult<Credentials, LoginError>;
}
