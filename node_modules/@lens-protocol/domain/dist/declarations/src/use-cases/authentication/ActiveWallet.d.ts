import { ICredentials, Wallet } from "../../entities/index.js";
export interface ICredentialsReader {
    getCredentials(): Promise<ICredentials | null>;
}
export interface IReadableWalletGateway {
    getByAddress(address: string): Promise<Wallet | null>;
}
export declare class ActiveWallet {
    private credentialsReader;
    private walletGateway;
    constructor(credentialsReader: ICredentialsReader, walletGateway: IReadableWalletGateway);
    requireActiveWallet(): Promise<Wallet>;
}
