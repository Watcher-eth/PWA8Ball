import { aZ as NATIVE_TOKEN_ADDRESS } from './index-a58ee834.esm.js';
import { i as isNativeToken } from './fetchCurrencyValue-ce3af0ef.esm.js';

/**
 * @internal
 */
function cleanCurrencyAddress(currencyAddress) {
  if (isNativeToken(currencyAddress)) {
    return NATIVE_TOKEN_ADDRESS;
  }
  return currencyAddress;
}

export { cleanCurrencyAddress as c };
